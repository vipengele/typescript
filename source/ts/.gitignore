# Build artifacts
dist/
.turbo/

# Generated by tests and tooling. The project carries these itself because Biome
# reads the .gitignore beside its config, not the repository root's.
coverage/
node_modules/
*.tsbuildinfo
.vitest/
